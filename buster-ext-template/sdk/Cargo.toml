[package]
name = "buster-ext-sdk"
version = "0.1.0"
edition = "2021"
description = "Guest SDK for building Buster IDE extensions in Rust"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib", "rlib"]
